import { ogImage, size, contentType } from "@/lib/og";

export { size, contentType };
export const alt = "Air Barrier Special Inspections — LES NRG";

export default function Image() {
  return ogImage("Air Barrier Special Inspections");
}
